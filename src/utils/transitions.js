export default {
	// like ease-in-out but with stronger slowing at the end
	// for sizes, positions
	easeInStrongEaseOut: 'cubic-bezier(0.4, 0, 0.2, 1)',

	// fast linear at the beginning, very strong slowing at the end
	// for colors
	strongEaseOut: 'cubic-bezier(.25, .8, .25, 1)'
}
